# Domain docs

Read root CONTEXT.md and relevant docs/adr/ decisions before
exploring. If absent, proceed silently; create them lazily
through domain-modeling.

Use glossary terminology. Surface conflicts with ADRs rather
than silently overriding them.
